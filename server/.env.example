# 이 파일에는 PORT/HOST 같은 실제 값을 두지 않습니다.
# `server` 폴더에 `.env`를 새로 만든 뒤, 아래 이름으로 로컬에서만 값을 입력하세요.
# 예: PORT=5174 / HOST=127.0.0.1
#
# Do not commit real secrets. Copy this file to `.env` and fill values only on your machine.
