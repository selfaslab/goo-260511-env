# 비밀값·연결 문자열은 Git에 올리지 마세요.
# 이 파일을 `.env`로 복사한 뒤, 로컬에서만 아래 이름에 맞춰 값을 직접 입력하세요.
#
# Never commit `.env`. After copying to `.env`, set only on your machine:
#   OPENAI_API_KEY, VITE_OPENAI_API_KEY, GITHUB_TOKEN, VITE_GITHUB_PAT, DATABASE_URL, APP_SECRET
